/**
 * Kept as the entry point App.tsx imports; the remote app itself lives in
 * ./remote (shell + tabs), shared by the browser and the Android companion.
 */
export { RemoteApp as MobileClient } from './remote/RemoteApp'
