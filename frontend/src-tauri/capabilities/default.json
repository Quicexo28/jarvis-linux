{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions. La lista de `windows` es una ACL por LABEL: una ventana que no este aqui recibe 'Command plugin:event|listen not allowed by ACL' al suscribirse, la promesa se rechaza y sus efectos mueren en silencio \u2014 sin listener, sin contenido y sin error visible mas alla de esa linea en consola.",
  "windows": [
    "main",
    "ptt-overlay",
    "wall"
  ],
  "permissions": [
    "core:default",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-is-registered"
  ]
}
