Texturas planetarias del visor de simulaciones (kind: 'simulation').

Gitignoreadas (~7,5 MB de binarios). Regenerar con:
  ./scripts/fetch-sim-textures.sh

Fuente: solarsystemscope.com — CC BY 4.0
  https://www.solarsystemscope.com/textures/

Ojo: `vite build` NO vacía public/ (a diferencia de dist/), así que estos
ficheros se copian tal cual al bundle. Se sirven en /textures/<archivo>.
